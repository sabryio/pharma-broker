{
  "name": "pharma-gateway",
  "version": "0.1.0",
  "description": "PharmaBroker AI Gateway - TypeScript service for AI SDK and dashboard",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target node",
    "start": "bun run dist/index.js",
    "test": "bun test"
  },
  "dependencies": {
    "@ai-sdk/openai": "^2.0.88",
    "ai": "^5.0.115",
    "hono": "^4.11.1",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@types/bun": "1.3.5",
    "typescript": "^5.9.3"
  }
}
